[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "myagents"
version = "0.1.0"
description = "Local multi-agent terminal orchestrator"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Bryant Yang" },
]
dependencies = [
  "httpx>=0.27,<1",
  "mcp>=1.27,<2",
  "starlette>=1,<2",
  "textual>=1.0",
  "uvicorn>=0.52,<1",
]

[project.scripts]
myagents = "main:main"
myagents-mcp = "myagents_mcp:main"

[tool.setuptools]
include-package-data = true
py-modules = [
  "agent_readiness",
  "attached_tui",
  "clipboard_image",
  "collaboration",
  "context_lifecycle",
  "discussion",
  "host",
  "host_backend",
  "main",
  "myagents_mcp",
  "orchestrator",
  "runtime_daemon",
  "session_catalog",
  "session_manager",
  "session_roles",
  "tui_activity",
  "tui_completion",
  "tui_markdown",
  "tui_status",
  "workflow",
]

[tool.setuptools.packages.find]
where = ["."]
include = [
  "acp*",
  "adapters*",
  "codex_app_server*",
  "control*",
  "dsh_acp*",
  "native_agent*",
  "pi_rpc*",
  "remote_control*",
  "storage*",
  "workspace*",
]
exclude = ["tests*"]

[tool.setuptools.package-data]
dsh_acp = [
  "plugin/*.json",
  "plugin/*.md",
  "plugin/*.mjs",
  "plugin/*.yml",
  "plugin/scripts/*.mjs",
  "plugin/src/*.ts",
  "plugin/tests/*.ts",
]
remote_control = [
  "static/*.html",
  "static/*.js",
]
